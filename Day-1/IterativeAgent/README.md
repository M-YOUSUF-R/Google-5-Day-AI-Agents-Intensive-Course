# Iterative Story Refinement Agent
### **description:**
it is an agent , that will run for a given iteration loop , and sequentially run the `sub_agents` list to achieve refined output from the Agent.\
This Agent use `Gemini` as a LLM model
